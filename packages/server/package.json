{
  "name": "@aart/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AART server package (architecture §0.1-0.3, §6, §14): control plane (aart server), runtime-plane worker bin (aart worker), the 13 trigger adapters, the scheduler ticker + lease/reclaim/graceful-shutdown runtime, bundle production logic, and environment/deployment/promotion records.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@aart/engine": "workspace:*",
    "@aart/evidence": "workspace:*",
    "@aart/expr": "workspace:*",
    "@aart/governance": "workspace:*",
    "@aart/store": "workspace:*",
    "@aart/types": "workspace:*",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
